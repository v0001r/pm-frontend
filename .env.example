# Local backend
VITE_API_URL=http://localhost:3001/api

# Vercel → Project Settings → Environment Variables (all environments):
# VITE_API_URL=https://your-backend.onrender.com/api
#
# Vercel build: use Framework Preset "TanStack Start" (or vercel.json).
# Do NOT set Output Directory to "dist" — Nitro writes .vercel/output automatically.
cd